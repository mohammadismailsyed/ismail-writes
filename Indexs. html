export default function OrganicFarmingWebsite() { const pros = [ 'Healthy and chemical-free food', 'Improves soil fertility', 'Protects environment and biodiversity', 'Reduces water pollution', 'Safer for farmers and consumers', ];

const cons = [ 'Lower yield during early stages', 'Higher product prices', 'Pest management challenges', 'Requires more labor', 'Lack of awareness among farmers', ];

const solutions = [ 'Use compost and biofertilizers', 'Adopt AI and smart farming tools', 'Government subsidies and farmer support', 'Online education and training programs', 'Direct farmer-to-customer marketplaces', ];

const alternatives = [ { title: 'Hydroponics', desc: 'Growing plants without soil using nutrient-rich water.', }, { title: 'Aquaponics', desc: 'Combines fish farming with plant cultivation.', }, { title: 'Vertical Farming', desc: 'Growing crops in stacked indoor systems.', }, { title: 'Precision Agriculture', desc: 'Uses AI, drones, and sensors for smart farming.', }, ];

return ( <div className="min-h-screen bg-green-50 text-gray-800"> {/* Hero Section */} <section className="bg-gradient-to-r from-green-700 to-green-500 text-white py-20 px-6 text-center"> <h1 className="text-5xl font-bold mb-6"> Organic Farming </h1> <p className="text-xl max-w-3xl mx-auto mb-8"> Healthy Soil, Healthy Food, Healthy Future. Sustainable farming for a better world. </p>

<div className="flex justify-center gap-4 flex-wrap">
      <button className="bg-white text-green-700 px-6 py-3 rounded-2xl font-semibold shadow-lg hover:scale-105 transition">
        Learn More
      </button>

      <button className="bg-green-900 px-6 py-3 rounded-2xl font-semibold shadow-lg hover:scale-105 transition">
        Explore Solutions
      </button>
    </div>
  </section>

  {/* About Section */}
  <section className="py-16 px-6 max-w-6xl mx-auto">
    <h2 className="text-4xl font-bold mb-6 text-green-700">
      What is Organic Farming?
    </h2>

    <p className="text-lg leading-8">
      Organic farming is a sustainable agricultural system that avoids
      synthetic fertilizers, pesticides, and harmful chemicals. It focuses
      on natural farming techniques such as composting, crop rotation,
      biological pest control, and eco-friendly cultivation methods.
    </p>
  </section>

  {/* Pros and Cons */}
  <section className="py-16 px-6 bg-white">
    <div className="max-w-6xl mx-auto grid md:grid-cols-2 gap-10">
      <div className="bg-green-100 p-8 rounded-3xl shadow-lg">
        <h3 className="text-3xl font-bold text-green-700 mb-6">
          Advantages
        </h3>

        <ul className="space-y-4">
          {pros.map((item, index) => (
            <li
              key={index}
              className="bg-white p-4 rounded-xl shadow"
            >
              ✅ {item}
            </li>
          ))}
        </ul>
      </div>

      <div className="bg-red-100 p-8 rounded-3xl shadow-lg">
        <h3 className="text-3xl font-bold text-red-700 mb-6">
          Challenges
        </h3>

        <ul className="space-y-4">
          {cons.map((item, index) => (
            <li
              key={index}
              className="bg-white p-4 rounded-xl shadow"
            >
              ⚠️ {item}
            </li>
          ))}
        </ul>
      </div>
    </div>
  </section>

  {/* Solutions Section */}
  <section className="py-16 px-6 max-w-6xl mx-auto">
    <h2 className="text-4xl font-bold text-center text-green-700 mb-12">
      Solutions for Organic Farming Problems
    </h2>

    <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {solutions.map((item, index) => (
        <div
          key={index}
          className="bg-white p-6 rounded-3xl shadow-lg hover:shadow-2xl transition"
        >
          <h3 className="text-2xl font-semibold mb-4 text-green-700">
            Solution {index + 1}
          </h3>

          <p className="text-lg">{item}</p>
        </div>
      ))}
    </div>
  </section>

  {/* Alternatives */}
  <section className="py-16 px-6 bg-green-100">
    <div className="max-w-6xl mx-auto">
      <h2 className="text-4xl font-bold text-center text-green-700 mb-12">
        Alternative Modern Farming Methods
      </h2>

      <div className="grid md:grid-cols-2 gap-8">
        {alternatives.map((item, index) => (
          <div
            key={index}
            className="bg-white p-8 rounded-3xl shadow-lg"
          >
            <h3 className="text-2xl font-bold text-green-700 mb-4">
              {item.title}
            </h3>

            <p className="text-lg leading-7">{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  {/* Why Backward */}
  <section className="py-16 px-6 max-w-6xl mx-auto">
    <h2 className="text-4xl font-bold text-green-700 mb-10 text-center">
      Why Organic Farming is Still Backward?
    </h2>

    <div className="grid md:grid-cols-2 gap-8">
      <div className="bg-white p-8 rounded-3xl shadow-lg">
        <ul className="space-y-5 text-lg leading-8">
          <li>• Lack of awareness among farmers</li>
          <li>• Slow transition from chemical farming</li>
          <li>• Weak supply chain and marketing</li>
          <li>• Limited technology access</li>
          <li>• High organic product pricing</li>
        </ul>
      </div>

      <div className="bg-green-700 text-white p-8 rounded-3xl shadow-lg">
        <h3 className="text-3xl font-bold mb-6">
          Comeback Strategy
        </h3>

        <ul className="space-y-4 text-lg leading-8">
          <li>✔ AI-powered smart farming</li>
          <li>✔ Government support programs</li>
          <li>✔ Better online marketing</li>
          <li>✔ Youth involvement in agri-tech</li>
          <li>✔ Consumer awareness campaigns</li>
        </ul>
      </div>
    </div>
  </section>

  {/* Future Section */}
  <section className="py-20 px-6 bg-gradient-to-r from-green-800 to-green-600 text-white text-center">
    <h2 className="text-5xl font-bold mb-8">
      Future of Organic Farming
    </h2>

    <p className="text-xl max-w-4xl mx-auto leading-9">
      Organic farming can become one of the most important solutions for
      climate change, food safety, and sustainable agriculture. By combining
      modern technology with eco-friendly practices, the future of farming
      can be healthier, smarter, and more sustainable.
    </p>
  </section>

  {/* Footer */}
  <footer className="bg-black text-white py-8 text-center">
    <p className="text-lg">
      © 2026 Organic Farming Awareness Website | Built for Sustainable
      Agriculture
    </p>
  </footer>
</div>

); }
